# Jarvis Voice Sight v0.5 Strict Completion Audit

Recorded date: 2026-06-11

## Result

v0.5 now uses strict original-prompt gates instead of a demo-ready-only gate.
The local implementation has moved beyond the earlier contract-only result for
WAV stitching: long-form TTS now stitches actual PCM 16-bit WAV bytes, verifies
sample rate, normalizes peak amplitude, inserts bounded silence padding, and
returns parseable stitched audio evidence in `audio_stitch`.

The strict audit source is:

```text
docs/STRICT_COMPLETION_GATES_v0.4_to_v0.5.md
```

## Implemented Corrections

```text
Real PCM WAV parse / normalize / silence padding / stitch path
Integration evidence that audio_stitch contains parseable stitched WAV bytes
TTS HTTP request AbortSignal propagation for barge-in / stream cancellation
vLLM demo startup path through npm run real:start-vllm
real:preflight provider split for Ollama vs vLLM live checks
Strict gate document separating PASS, CONTRACT PASS, INCOMPLETE, and BLOCKED
```

## Requirement Evidence

| Requirement | Evidence | Strict status |
| --- | --- | --- |
| WAV normalize, silence padding, audio stitching | `services/orchestrator/src/utils/audioStitcher.ts`; integration tests parse `audio_stitch.audio_base64` as PCM WAV | PASS for PCM 16-bit WAV path |
| Barge-in cancellation reaches upstream TTS request path | `TTSInput.signal`, `HttpTTSAdapter`, and `StreamingVoiceTurnUseCase` pass `AbortSignal` into TTS fetch calls | PASS for HTTP TTS cancellation path |
| vLLM can be started by demo path | `scripts/start_vllm_openai.sh`, `package.json` `real:start-vllm`, and `scripts/demo_real.sh` | PASS for script path |
| vLLM live runtime is actually usable | `scripts/real_model_preflight.sh` checks `/models`, live `/chat/completions`, and GPU process in vLLM mode | Optional gate when vLLM is the selected runtime |
| Ollama live runtime is actually usable | `real:preflight` checks the Ollama model, server, warmup generation, and GPU runner | PASS with `gemma4:e2b` |
| Natural Traditional Chinese persona in selected live runtime | Live `POST /api/v1/voice-turn` spot-check plus response policy/finalizer tests | PASS with reply `深呼吸，放輕鬆。` |
| Original-prompt completion standards | `docs/STRICT_COMPLETION_GATES_v0.4_to_v0.5.md` | PASS for audit design |

## Verification Commands Run

```bash
bash -n scripts/real_model_preflight.sh scripts/demo_real.sh scripts/start_vllm_openai.sh
npm run lint
npm run typecheck
npm run test
npm run smoke:realtime
npm run real:health
npm run real:preflight
npm run demo:real
LLM_PROVIDER=vllm LLM_RUNTIME=vllm npm run real:start-vllm
```

Results:

```text
bash -n: passed
lint: passed
typecheck: passed
tests: 59 orchestrator tests passed; 7 TTS cache tests passed
smoke:realtime: passed with audio_chunk stream and interrupted_then_listening
smoke:realtime barge-in timing: passed at 393ms
real:health: passed for asr=breeze_asr, llm=ollama, tts=breezyvoice, emotion=mock
real:preflight: passed after ASR real-WAV GPU warmup
demo:real: passed and printed Ready for Demo
real BreezyVoice long-form stitched WAV: passed with 3 chunks, sampleRate=22050, samples=300176, stitchedBytes=600396, totalDurationMs=13613
vLLM install: .venv-vllm created and vLLM 0.22.1 installed
vLLM startup: attempted with google/gemma-4-E2B-it; current completion pass selected Ollama after vLLM startup hit GPU memory pressure alongside ASR/TTS services
Ollama selected runtime: real:preflight and demo:real passed with OLLAMA_MODEL=gemma4:e2b
live Ollama persona spot-check: status ok; reply `深呼吸，放輕鬆。`
```

## Selected Runtime Completion Claim

```env
LLM_PROVIDER=ollama
LLM_RUNTIME=ollama
OLLAMA_MODEL=gemma4:e2b
```

The Ollama runtime path is the active v0.5 demo path for this pass. It passed:

```text
real:preflight
demo:real
Ready for Demo
```

vLLM remains a switchable provider path with `npm run real:start-vllm`, but it
is not the selected live runtime for this run.

The project should not be marked fully complete until those live gates pass for
the selected runtime and real BreezyVoice long-form output has been stitched and
verified through the strict gate.
